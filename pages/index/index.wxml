<!--index.wxml-->
<view class="container">
  <videos wx:if="{{active === 0}}"></videos>
  <playground wx:if="{{active === 1}}"></playground>
  <profile wx:if="{{active === 2}}"></profile>
  <van-tabbar active="{{ active }}" bind:change="onChange">
    <van-tabbar-item icon="tv-o">视频</van-tabbar-item>
    <van-tabbar-item icon="wap-home-o">广场</van-tabbar-item>
    <van-tabbar-item icon="manager-o">我的</van-tabbar-item>
  </van-tabbar>
</view>